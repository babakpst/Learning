
// babak poursartip
// Aug 28, 2022

#include <iostream>
#include <limits>
#include <vector>
#include <algorithm>


using namespace std;

/**
 * // This is the ArrayReader's API interface.
 * // You should not implement it, or speculate about its implementation
 * class ArrayReader {
 *   public:
 *     int get(int index);
 * };
 */

class Solution {
public:
    int search(const ArrayReader& reader, int target) {
f the target value is equal to the middle element - we're done.
  std::cout << " starts ... \n";

  const int reader
  int target
 
  Solution t;

  int out = t.search(reader, target);
  
  cout << " answer: " << out << endl;
  
  std::cout << " done \n";
  return 0;
}