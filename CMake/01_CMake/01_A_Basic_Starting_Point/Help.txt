

- Create the CMakeLists.txt file in the root directory where your cpp codes are. 
- Everything in lower case. Mixed cases are supported by cmake. 
- Create the build directory.
- cd build
- cmake ../
- cmake --build .

